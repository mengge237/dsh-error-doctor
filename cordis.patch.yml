# dsh-error-doctor bundle patch
#
# 装（web 或任意 profile；装完要完全重启 dsh web 才生效）：
#   dsh plugin --profile web add link:<这个目录的绝对路径>
# 卸：
#   dsh plugin --profile web remove dsh-error-doctor
- insert:
    - id: dsh-error-doctor
      name: 'dsh-error-doctor'
